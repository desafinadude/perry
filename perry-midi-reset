#!/bin/sh
# Re-enumerate USB MIDI devices without restarting the browser.
# Run this after perry is already open and waiting for MIDI input.
set -e
echo "Reloading USB audio module..."
sudo modprobe -r snd-usb-audio && sudo modprobe snd-usb-audio
echo "Done. MIDI devices should reappear in the browser momentarily."
